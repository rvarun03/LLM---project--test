{
  "//": "The device agent is CommonJS (it is downloaded and run standalone with plain node). The root package.json sets \"type\": \"module\", which would otherwise make `node public/automatiqa-agent.js` fail with 'require is not defined'. This scopes this directory back to CommonJS.",
  "type": "commonjs"
}
